#server port
server:
  port: 8080

#data base
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pc?createDatabaseIfNotExist=true
    username: root
    password: ckwogus0118#

  #thymeleaf
  thymeleaf:
    cache: false

  #spring data jpa
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: update

#업로드 파일 사이즈
spring.servlet.multipart.max-file-size: 100MB
spring.servlet.multipart.max-request-size: 100MB
